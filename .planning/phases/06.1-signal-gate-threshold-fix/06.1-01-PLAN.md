---
phase: 06.1-signal-gate-threshold-fix
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - signals/generator.py
autonomous: true
requirements:
  - ENGINE-03
gap_closure: true

must_haves:
  truths:
    - "STABILITY_THRESHOLD in signals/generator.py equals 70.0"
    - "CORRELATION_THRESHOLD in signals/generator.py equals 0.65"
    - "passes_gate(70.0, 0.66) returns False (strict > not >=)"
    - "passes_gate(70.1, 0.66) returns True"
    - "All 12 pre-existing test failures in test_signals_generator.py are resolved"
    - "No regression in previously passing tests"
  artifacts:
    - path: "signals/generator.py"
      provides: "ENGINE-03 hard gate enforcement"
      contains: "STABILITY_THRESHOLD = 70.0"
    - path: "signals/generator.py"
      provides: "ENGINE-03 hard gate enforcement"
      contains: "CORRELATION_THRESHOLD = 0.65"
  key_links:
    - from: "signals/generator.py::passes_gate"
      to: "STABILITY_THRESHOLD / CORRELATION_THRESHOLD constants"
      via: "direct constant reference"
      pattern: "stability_score > STABILITY_THRESHOLD"
---

<objective>
Fix ENGINE-03 threshold mismatch in signals/generator.py. The hard gate constants
STABILITY_THRESHOLD and CORRELATION_THRESHOLD were shipped at 50.0/0.50 instead of the
milestone-required 70.0/0.65. This plan corrects the constants and aligns the
determine_sizing_tier function signature and thresholds to match what the tests expect.

Purpose: Close the single open gap from the v1.0 milestone audit. 47/48 requirements are
satisfied; this fix closes ENGINE-03 to reach 48/48.

Output: signals/generator.py with correct constants; pytest test_signals_generator.py passes
with 0 failures.
</objective>

<execution_context>
@C:/Users/USER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/USER/.planning/ROADMAP.md
@/c/Users/USER/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix threshold constants, gate docstring, and sizing tier in signals/generator.py</name>
  <files>signals/generator.py</files>
  <action>
Make the following targeted changes to signals/generator.py. Do NOT rewrite the file —
edit only the specific lines described.

**Change 1 — Module docstring threshold table (lines 3-13):**
Update the correlation and stability threshold commentary to reflect 0.65 as gate minimum
and 70 as gate minimum. Specifically:
  - Change "≥ 0.50 → moderate, generally acceptable   (gate minimum)" to
    "≥ 0.65 → strong — gate minimum"
  - Change "≥ 0.50 → moderate, generally acceptable     (gate minimum)" to
    "≥ 70 → strong — gate minimum"
  - Update the SIZING TIERS block to match the new one-argument tier logic described below.

**Change 2 — Gate constants (lines 43-44):**
  STABILITY_THRESHOLD: float = 70.0      # ENGINE-03 hard gate minimum (was 50.0)
  CORRELATION_THRESHOLD: float = 0.65   # ENGINE-03 hard gate minimum (was 0.50)

**Change 3 — Sizing tier private constants (lines 47-50):**
The tests call determine_sizing_tier with ONE argument (stability_score only) and expect:
  - stability_score > 85  -> 'full'
  - stability_score > 70  -> 'half'   (i.e., passes gate but not full)
  - anything else         -> 'quarter' (unreachable from gate, kept for completeness)

Replace the four _STABILITY_STRONG/_STABILITY_MODERATE/_CORRELATION_STRONG/_CORRELATION_MODERATE
constants with two:
  _SIZING_FULL_THRESHOLD: float = 85.0    # stability > 85 -> 'full'
  _SIZING_HALF_THRESHOLD: float = 70.0    # stability > 70 -> 'half' (at gate level)

**Change 4 — determine_sizing_tier signature and body:**
Change the function from two-argument `(stability_score, correlation_strength)` to
one-argument `(stability_score)`:

  def determine_sizing_tier(stability_score: float) -> str:
      """Map stability score to sizing tier (SIGNAL-01).

      Tier breakpoints (strict >):
        'full':    stability_score > 85   (high confidence)
        'half':    stability_score > 70   (passes gate — moderate to strong)
        'quarter': stability_score <= 70  (at gate boundary — kept for completeness)
      """
      if stability_score > _SIZING_FULL_THRESHOLD:
          return 'full'
      if stability_score > _SIZING_HALF_THRESHOLD:
          return 'half'
      return 'quarter'

**Change 5 — passes_gate docstring:**
Update the docstring to correctly state:
  "stability_score > 70 AND abs(correlation_strength) > 0.65"
(Remove the wrong "stability_score >= 40 AND abs(correlation_strength) >= 0.30" line.)

**Change 6 — passes_gate body (line 65):**
The comparison operator is already >= which is wrong given the strict > requirement.
Change to:
  return stability_score > STABILITY_THRESHOLD and abs(correlation_strength) > CORRELATION_THRESHOLD

Note: tests test_passes_gate_false_at_stability_boundary and
test_passes_gate_false_at_correlation_boundary both rely on strict > (not >=). The current
code uses >=, which also needs fixing.

**Change 7 — Call site in generate_signal:**
On the line `sizing_tier = determine_sizing_tier(stability_score, correlation_strength)`,
remove the second argument:
  sizing_tier = determine_sizing_tier(stability_score)
  </action>
  <verify>
    <automated>cd /c/Users/USER/lead-lag-quant && python -m pytest tests/test_signals_generator.py -v 2>&amp;1 | tail -20</automated>
    <manual>Confirm output shows "0 failed" and "passed" for all tests including:
    - test_passes_gate_false_at_stability_boundary
    - test_passes_gate_false_at_correlation_boundary
    - test_stability_threshold_value
    - test_correlation_threshold_value
    - test_sizing_tier_full_above_85
    - test_sizing_tier_half_between_70_and_85
    Also run: grep "STABILITY_THRESHOLD = " signals/generator.py — must show 70.0
    And:      grep "CORRELATION_THRESHOLD = " signals/generator.py — must show 0.65</manual>
  </verify>
  <done>pytest tests/test_signals_generator.py exits 0 with 0 failures (was 12). STABILITY_THRESHOLD=70.0 and CORRELATION_THRESHOLD=0.65 are present in signals/generator.py.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no regression in full test suite</name>
  <files></files>
  <action>
Run the full test suite to confirm no regression was introduced by the generator.py changes.
The only file changed is signals/generator.py; the call site in generate_signal now passes
one argument to determine_sizing_tier instead of two.

Check for any other callers of determine_sizing_tier outside test files:
  grep -rn "determine_sizing_tier" /c/Users/USER/lead-lag-quant --include="*.py"

If any non-test caller passes two arguments, update it to pass only stability_score.

Then run:
  cd /c/Users/USER/lead-lag-quant && python -m pytest tests/ -v --tb=short 2>&amp;1 | tail -30

The audit noted 1 pre-existing failure in test_engine_detector.py (likely threshold-related).
If it clears with the fix, count it as a bonus. If it remains, note it but do not block.
  </action>
  <verify>
    <automated>cd /c/Users/USER/lead-lag-quant && python -m pytest tests/test_signals_generator.py tests/test_engine_detector.py -v --tb=short 2>&amp;1 | tail -20</automated>
    <manual>test_signals_generator.py: 0 failures. Note whether test_engine_detector.py failure count changed.</manual>
  </verify>
  <done>test_signals_generator.py passes fully. No new failures introduced in any other test file compared to pre-fix baseline.</done>
</task>

</tasks>

<verification>
Confirm ENGINE-03 gap is closed:

1. grep "STABILITY_THRESHOLD" signals/generator.py   — must show 70.0
2. grep "CORRELATION_THRESHOLD" signals/generator.py — must show 0.65
3. python -m pytest tests/test_signals_generator.py  — must exit 0 with 0 failures
4. python -c "from signals.generator import passes_gate; assert passes_gate(70.0, 0.66) is False; assert passes_gate(70.1, 0.66) is True; print('gate OK')"
</verification>

<success_criteria>
- STABILITY_THRESHOLD = 70.0 in signals/generator.py (was 50.0)
- CORRELATION_THRESHOLD = 0.65 in signals/generator.py (was 0.50)
- passes_gate uses strict > (not >=) for both thresholds
- determine_sizing_tier accepts one argument (stability_score only)
- pytest tests/test_signals_generator.py: 0 failures (was 12)
- No new failures introduced in other test files
- ENGINE-03 requirement satisfied: 48/48 v1.0 milestone requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-signal-gate-threshold-fix/06.1-01-SUMMARY.md`
with standard summary frontmatter including requirements-completed: [ENGINE-03].
</output>